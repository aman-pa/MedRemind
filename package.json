{
  "name": "medremind",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "npm install --prefix backend || true",
    "build": "npm install --prefix backend || true",
    "start": "node backend/server.js || node server.js"
  }
}
